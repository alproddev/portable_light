<%= heading("New #{singular_resource}") %>

<%= cta link_to("&laquo; Cancel", collection_path) %>

<% semantic_form_for resource, :url => collection_path, :html => { :multipart => true } do |form| %>
  <%= render :partial => 'form', :locals => { :form => form } %>
  <%= form.buttons %>
<% end %>
